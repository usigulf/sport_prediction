[pytest]
# Quick runs without coverage (repo root pytest.ini adds --cov and fail-under).
# Usage from backend/: PYTHONPATH=. pytest -c pytest-fast.ini tests/
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
