# Install after backend deps: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.27.0
# Fast subset (no cov): PYTHONPATH=. pytest -c pytest-fast.ini tests/test_stats_trust.py
