# Archived Expo app (not maintained)

This directory is an **alternate / experimental** client (different navigation and dependencies).  

**Canonical mobile app:** use the **`/mobile`** workspace at repo root (`sport-prediction-mobile`).  

Do not add features here unless you deliberately merge back into `/mobile`.
