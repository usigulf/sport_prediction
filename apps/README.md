# `apps/` — stub only

The experimental Expo client that lived at `apps/mobile/` was archived to
[`archive/apps-mobile/`](../archive/apps-mobile/) (external audit #12).

**Use [`mobile/`](../mobile/) for all product development.**
