# Nginx: Privacy Policy URL at /privacy
#
# Prereq: /var/www/octobetiq/privacy.html (repo: web/privacy.html)
# Repeat in www HTTPS server block if apex redirects to www.

location = /privacy {
    root /var/www/octobetiq;
    rewrite ^ /privacy.html break;
    default_type text/html;
}
